{{define "header"}}
<header class="border-b border-gray-200">
    <div class="max-w-[900px] mx-auto px-5 py-4 flex items-center justify-between">
        <a href="/" class="font-mono font-bold text-xl text-gray-900 no-underline">
            Harry Day<span class="text-accent">{}</span>
        </a>
        <nav class="flex gap-6">
            <a href="/" class="text-gray-500 hover:text-accent font-medium transition-colors text-sm">Home</a>
            <a href="{{.BlogRoot}}" class="text-gray-500 hover:text-accent font-medium transition-colors text-sm">Blog</a>
            <a href="{{.BlogRoot}}tags" class="text-gray-500 hover:text-accent font-medium transition-colors text-sm">Tags</a>
        </nav>
    </div>
</header>
{{end}}
